import { AnneesDepuisPipe } from './annees-depuis-pipe';

describe('AnneesDepuisPipe', () => {
  it('create an instance', () => {
    const pipe = new AnneesDepuisPipe();
    expect(pipe).toBeTruthy();
  });
});
